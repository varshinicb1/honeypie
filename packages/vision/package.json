{
  "name": "@honeypie/vision",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "dependencies": {
    "@honeypie/core": "workspace:*",
    "@honeypie/plugin-sdk": "workspace:*"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --config ../../vitest.config.ts src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
