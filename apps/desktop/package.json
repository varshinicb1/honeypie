{
  "name": "desktop",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "bundle:cli": "node scripts/bundle-cli.mjs",
    "build": "npm run bundle:cli && tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-dialog": "^2"
  },
  "devDependencies": {
    "@honeypie/cli": "workspace:*",
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.28.1",
    "vite": "^6.0.3",
    "typescript": "~7.0.2"
  }
}
